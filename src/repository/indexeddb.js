export const saveLevelToIndexedDb = (level) => {
  console.log('level saved to indexedDb: ', level)
}

export const getLevelFromIndexedDb = () => {
  console.log('get level from indexedDb')
  return 2
}